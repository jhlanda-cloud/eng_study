# 영어 명언 학습 앱 — PRD

## 1. 개요
영어 명언(Quote)을 읽고, 듣고, 외워서 직접 입력해보는 개인용 영어 학습 미니 앱이다. React Native(Expo SDK 54) 기반으로 제작하며, React Native를 처음 배우는 사용자가 오늘 실습 시간 내에 바이브코딩으로 완성하는 것을 목표로 한다. 명언 데이터는 DummyJSON Quotes API에서 가져오고, 한국어 번역은 DeepL API를 Vercel 서버리스 함수로 프록시 호출해 API 키를 앱에 노출하지 않는다. 배포는 EAS Update(Expo Go, Android/iOS)와 EAS Build APK(Android 전용) 두 방식을 병행 지원한다.

## 2. 문제정의
- 영어 명언을 단순히 "읽기만" 하면 암기로 이어지지 않고, 발음·의미·문장 구조를 동시에 익히기 어렵다.
- 시중 명언 앱은 대부분 광고/로그인/구독이 있어 개인 학습용으로 가볍게 쓰기 번거롭다.
- React Native를 처음 접하는 입문자가 "화면-데이터-외부 API-배포"까지 이어지는 실전 흐름을 짧은 시간 안에 손으로 경험할 학습 자료가 부족하다.
- 결과적으로, 키 관리·번역 연동·듣기·자가 테스트·배포까지 한 번에 실습할 수 있는 최소 기능의 개인용 앱이 필요하다.

## 3. 목표 (Goals / Non-goals)

### Goals
- 영어 명언 + 저자 + 한국어 번역을 한 화면에서 확인할 수 있다.
- 버튼 클릭으로 명언의 영어 발음을 들을 수 있다(기기 내장 TTS).
- 명언을 가리고 직접 타이핑하여 외운 내용을 스스로 확인할 수 있다.
- DeepL API 키는 서버(Vercel)에만 보관하고 클라이언트 앱에는 절대 노출하지 않는다.
- EAS Update(Expo Go)와 EAS Build APK, 두 가지 배포 방식을 모두 실제로 성공시킨다.
- 카카오톡 공유 버튼(로그인 없는 JS SDK 방식)을 통해 앱(또는 랜딩페이지)을 공유할 수 있다.
- React Native 입문자가 "가짜 데이터 → 서버 프록시 → 실제 API → 예외 처리" 순서로 실습을 완주한다.

### Non-goals
- 로그인/회원가입 기능 없음.
- OAuth 기반 카카오톡 자동 발송(메시지 API) 없음 — 이번 범위는 JS SDK 공유 버튼까지만.
- 학습 알림(푸시/리마인더) 기능 없음.
- 학습 기록·통계 저장(오답 이력, 진도 등) 없음.
- 다중 사용자, 서버 DB, 사용자 계정 시스템 없음.

## 4. 핵심 기능

| 기능 | 설명 | 우선순위 |
|---|---|---|
| 명언 표시 | DummyJSON Quotes API에서 랜덤 명언 1개와 저자를 가져와 표시 | P0 |
| 한국어 번역 표시 | Vercel 서버리스 함수(DeepL 프록시)를 호출해 명언을 한국어로 번역해 표시 | P0 |
| 새 명언 불러오기 | 버튼/새로고침으로 매번 랜덤 명언 1개를 새로 가져옴 | P0 |
| 발음 듣기 | "듣기" 버튼 클릭 시 기기 내장 음성합성(Speech)으로 영어 명언을 읽어줌 | P0 |
| 외워서 쓰기 & 정답 확인 | 명언을 가리고 사용자가 입력 → 원문과 느슨한 비교(대소문자·공백·문장부호 무시)로 정답 여부 표시 | P0 |
| 예외/에러 처리 | API 실패, 네트워크 오류, 번역 실패 시 사용자에게 안내 및 재시도 제공 | P1 |
| EAS Update 배포 | Expo Go로 설치 없이 QR/링크로 실행(Android·iOS) | P1 |
| EAS Build APK 배포 | Expo Go 없이 설치 가능한 APK 빌드(Android 전용) | P1 |
| 카카오톡 공유 버튼 | 랜딩페이지에서 로그인 없는 카카오 JS SDK로 공유하기 | P2 |

## 5. 사용자 시나리오
1. 사용자가 앱을 연다 (Expo Go 실행 또는 APK 설치 후 실행).
2. 화면에 영어 명언, 저자, 한국어 번역이 표시된다.
3. "듣기" 버튼을 눌러 영어 발음을 듣는다. 필요하면 다시 듣기를 반복한다.
4. "외워보기"를 눌러 명언 원문이 가려진다.
5. 사용자가 기억나는 문장을 입력창에 타이핑하고 "확인"을 누른다.
6. 앱이 입력값과 원문을 느슨하게 비교하여 정답/오답을 보여주고, 원문을 다시 노출한다.
7. "다음 명언" 버튼을 눌러 새로운 랜덤 명언을 불러와 1~6을 반복한다.
8. (선택) 랜딩페이지에서 카카오톡 공유 버튼을 눌러 지인에게 앱/링크를 공유한다.
9. (예외) 네트워크 오류나 번역 실패 시, 에러 메시지와 재시도 버튼이 표시되고 사용자는 재시도하거나 영어 명언만으로 학습을 계속할 수 있다.

## 6. 기술 요구사항
- **플랫폼**: Expo SDK 54 기반 React Native 앱 (Android, iOS).
- **명언 데이터**: DummyJSON Quotes API (`https://dummyjson.com/quotes` 등, 키 불필요) — 클라이언트에서 직접 호출.
- **번역**: DeepL API 호출은 클라이언트가 직접 하지 않고, Vercel 서버리스 함수(API 라우트)를 통해서만 프록시 호출한다. DeepL API 키는 Vercel 환경변수(서버 측)에만 저장하고 어떤 형태로도 클라이언트 코드/빌드에 포함하지 않는다.
- **음성 합성(듣기)**: 기기 내장 TTS 사용 (예: `expo-speech`), 영어(en) 발음으로 재생.
- **정답 비교 로직**: 사용자 입력과 원문을 소문자 변환, 앞뒤/중복 공백 정리, 문장부호 제거 후 비교(느슨한 비교).
- **개발 순서(필수 준수)**:
  1) 하드코딩된 가짜 명언/번역 데이터로 화면 완성
  2) Vercel 번역 프록시 서버 준비 및 단독 테스트
  3) 화면을 실제 DummyJSON API + 프록시 서버로 교체
  4) 네트워크/번역 실패 등 예외 처리 추가
- **배포**:
  - EAS Update + Expo Go: Android/iOS 공통, Expo Go 앱 설치 필요.
  - EAS Build (APK): Android 전용, Expo Go 불필요, 설치형 APK 산출물.
- **카카오톡 공유**: 로그인 없는 카카오 JS SDK 공유 방식(웹 환경 필요). 구체적 구현 위치(별도 웹 랜딩페이지 vs 인앱 웹뷰)는 Open Questions 참조.
- **보안**: DeepL 키 등 민감 정보는 `.env`/Vercel 환경변수로만 관리하며 Git에 커밋하지 않는다.

## 7. UI 구성
- **메인/학습 화면 (단일 화면 구성)**
  - 상단: 영어 명언 텍스트, 저자명
  - 그 아래: 한국어 번역 텍스트
  - 버튼 영역: [듣기] [외워보기] [다음 명언]
  - "외워보기" 클릭 시: 원문 영역이 가려지고, 텍스트 입력창 + [확인] 버튼 노출
  - 확인 후: 정답/오답 표시(색상 또는 아이콘) + 원문 다시 노출 + [다음 명언] 유도
  - 로딩 중: 로딩 인디케이터 표시
  - 에러 발생 시: 에러 메시지 + [재시도] 버튼
- **랜딩페이지(웹, 형태 미정)**
  - 앱 소개 문구, (선택) 스크린샷
  - 카카오톡 공유 버튼
  - (형태 확정 시) EAS Update 실행 링크/QR 또는 APK 다운로드 링크 안내

## 8. 성공지표
- 실습 시간 내에 4가지 핵심 흐름(명언 표시 → 번역 표시 → 듣기 → 외워서 정답 확인)이 실제 기기/Expo Go에서 오류 없이 동작한다.
- DeepL API 키가 클라이언트 코드/빌드 결과물 어디에도 노출되지 않음을 확인한다(코드 검색 기준).
- EAS Update(Expo Go)와 EAS Build(APK) 두 배포 방식 모두 최소 1회 이상 실제로 성공적으로 실행된다.
- 네트워크 차단/API 실패 상황에서 앱이 크래시 없이 에러 메시지를 보여준다.
- (참고용, 강제 목표 아님) 카카오톡 공유 버튼 클릭 시 공유 시트가 정상적으로 열린다.

## 9. 향후 고려사항
- 로그인/회원가입 및 사용자별 학습 기록 저장.
- 학습 알림/리마인더(푸시 알림) 기능.
- 오답 노트, 학습 진도·통계, 즐겨찾기 명언 저장.
- OAuth 기반 카카오톡 메시지 자동 발송(현재는 범위 밖).
- 명언 카테고리/난이도별 필터링, 검색.
- iOS용 자체 설치형 빌드(EAS Build IPA) 지원 검토.
- UI/UX 다듬기(애니메이션, 다크모드 등)는 이번 실습 범위 밖.

## Open Questions (미정 사항 — 임의로 추측하지 않음)
- **카카오톡 공유용 랜딩페이지의 정확한 형태**: 별도 정적 웹페이지(Vercel 배포)로 만들지, 인앱 웹뷰로 처리할지 아직 결정되지 않음. (카카오 JS SDK는 브라우저 환경에서만 동작하므로, 이 결정에 따라 6장 기술 요구사항과 7장 UI 구성의 랜딩페이지 항목이 구체화되어야 함)
- 랜딩페이지가 결정되면, 그 안에서 EAS Update 실행 방법(QR/딥링크) 또는 APK 다운로드 링크를 어떻게 안내할지도 함께 정해야 함.
- DummyJSON Quotes API의 정확한 엔드포인트/응답 스키마는 구현 단계에서 문서 확인 후 확정.
