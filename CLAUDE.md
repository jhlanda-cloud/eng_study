# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 Claude Code(claude.ai/code)에게 제공되는 가이드입니다.

## 프로젝트 현황

이 저장소에는 현재 `PRD.md`만 존재하며, 아직 앱 코드는 스캐폴딩되지 않았습니다(`package.json` 없음, Expo 프로젝트 없음). 첫 구현 단계는 Expo 프로젝트 자체를 생성하는 것입니다. 코드가 생기면 이 파일을 실제 빌드/린트/테스트 명령어와 검증된 아키텍처 내용으로 갱신하고, 아래 내용을 낡은 상태로 방치하지 않습니다.

## 이 프로젝트는 무엇인가

영어 명언 학습 앱(English Quote Learning App): 개인용(단일 사용자, 인증 없음)으로 **Expo SDK 54** 기반 React Native 앱을 만들어, 영어 명언을 읽고 듣고 외우는 것을 돕습니다. 전체 요구사항은 `PRD.md`에 있으며, 구현 전에 반드시 읽어야 합니다. 아래 요약은 코드 구조에 직접 영향을 주는 부분만 다룹니다.

## PRD가 기준 문서

이 프로젝트는 `PRD.md`를 기준으로 개발합니다. 새 기능을 구현하기 전에는 항상 `PRD.md`를 먼저 다시 확인합니다 — 관례나 "비슷한 앱들이 보통 이렇게 한다"는 추측으로 범위·우선순위·동작을 임의로 정하지 않습니다. `PRD.md`에 답이 없다면(Open Questions 참고) 추측하지 말고 사용자에게 먼저 물어봅니다.

## 필수 개발 순서

`PRD.md` §6에서 정한 순서로, 화면이 가짜 데이터로 잘 동작하는 걸 확인하기 전에는 실제 API로 건너뛰지 않습니다:

1. 하드코딩된 가짜 명언/번역 데이터로 화면을 먼저 완성하고, 화면이 실제로 잘 동작하는지 확인한다.
2. Vercel 번역 프록시 서버를 준비하고, 앱에 연결하기 전에 단독으로(curl/Postman 등) 테스트한다.
3. 화면을 실제 DummyJSON Quotes API와 실제 프록시 엔드포인트로 교체한다.
4. 화면과 실제 데이터 연결이 끝난 뒤, 마지막에 예외/에러 처리(네트워크 오류, 번역 실패 등)를 추가한다.

한 번에 다 만들어달라고 요청받아도 이 순서를 지킵니다 — 그렇게 요청받으면 위 4단계로 나눠서 진행하겠다고 먼저 말합니다.

## 아키텍처

- **클라이언트**: 단일 화면 Expo/React Native 앱(핵심 흐름에는 별도 내비게이션 라이브러리 불필요 — `PRD.md` §7 참고).
- **명언 데이터**: 클라이언트에서 DummyJSON Quotes API(`https://dummyjson.com/quotes`)를 직접 호출, 키 불필요. 한 번에 랜덤 명언 1개만 가져온다(목록/배치 아님).
- **번역**: 클라이언트는 DeepL을 절대 직접 호출하지 않습니다. `/api/translate` Vercel 서버리스 함수를 호출하고, 이 함수가 서버 측(Vercel 환경변수)에 DeepL API 키를 보관한 채 프록시 요청을 처리합니다. **DeepL 키는 RN 앱 코드, 클라이언트 설정, 빌드 결과물 어디에도 직접 쓰지 않습니다** — 항상 `/api/translate`를 통해서만 호출하고, 키는 Vercel 환경변수에만 둡니다. 이는 스타일 선호가 아니라 반드시 지켜야 하는 제약입니다.
- **음성 합성(듣기)**: 기기 내장 음성 합성(예: `expo-speech`) 사용, 영어 음성, 외부 TTS 서비스 사용 안 함.
- **정답 확인**: "외워서 쓰기" 기능은 사용자 입력과 원문을 *느슨하게* 비교합니다 — 소문자 변환, 앞뒤/중복 공백 정리, 문장부호 제거 후 비교. 완전 일치(엄격 비교)로 구현하지 않습니다.
- **배포**: 서로 독립적이며 둘 다 필요한 배포 경로 — EAS Update(Expo Go 경유, Android+iOS)와 EAS Build APK(Android 전용, Expo Go 불필요). 하나만 쓴다고 가정하지 않습니다.

## 기술 스택 제약

- Expo SDK는 **54로 고정**합니다 — 의존성 설치/업그레이드 시 SDK 54와 호환되지 않는 버전으로 올라가지 않도록 합니다.
- 패키지 설치·업그레이드는 항상 `npx expo install <패키지>`를 사용하고, `npm install <패키지>`는 사용하지 않습니다 — `expo install`이 고정된 SDK에 맞는 버전을 알아서 골라줍니다.
- 상태 관리는 **`useState`만** 사용합니다. 사용자가 명시적으로 요청하지 않는 한 Redux, Zustand, Context 기반 전역 상태 등 다른 상태 관리 라이브러리는 도입하지 않습니다.

## 오늘 작업 시 지켜야 할 방식

- 사용자는 오늘 React Native를 처음 배우는 시간입니다. 코드를 작성할 때는 무엇을 하는지뿐 아니라 *왜* 그렇게 짰는지도 짧게 설명을 곁들입니다.
- 코드를 수정한 뒤에는 항상 Expo Go로 QR코드를 스캔해서 확인해보라고 안내합니다.
- 코드를 짜기 전에 애매하거나 명확하지 않은 부분이 있으면 추측하지 말고 항상 먼저 사용자에게 물어봅니다.

## 명시적 비목표 (요청받지 않으면 추가하지 않음)

- 로그인/인증/사용자 계정 없음.
- OAuth 기반 카카오톡 메시지 자동 발송 없음(로그인 없는 카카오 JS SDK 공유 버튼만, 브라우저 환경 전용).
- 푸시 알림/학습 리마인더 없음.
- 학습 기록, 통계, 즐겨찾기 저장 없음.

## 미해결 사항 (`PRD.md` 참고)

- 카카오톡 공유용 "랜딩페이지"를 별도 정적 웹페이지로 만들지, 인앱 웹뷰로 처리할지 아직 결정되지 않았습니다. 확인 없이 임의로 구현하지 않습니다 — 카카오 JS SDK는 브라우저 환경에서만 동작하므로, 이 결정이 기술적 접근 방식과 공유 버튼의 위치 모두에 영향을 줍니다.
